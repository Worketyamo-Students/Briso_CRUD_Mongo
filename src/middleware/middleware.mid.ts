// import { Request,Response,NextFunction } from "express";
// import { regex } from "../controlleurs/user.controlleurs";

// const middlewares = {
//     authentication : (req:Request,res:Response,next:NextFunction)=>{
//         // regex.testRegex(regex.testRegex(email))
//         // regex.testRegex2(regex.testRegex(password))
//         next()
//     }
// }

// export default middlewares